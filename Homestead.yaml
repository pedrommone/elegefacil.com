---
ip: "192.168.10.10"
memory: 1024
cpus: 1

authorize: ~/.ssh/id_rsa.pub

keys:
    - ~/.ssh/id_rsa

folders:
    - map: ./
      to: /home/vagrant/

sites:
    - map: homestead.app
      to: /home/vagrant/www

variables:
    - key: APP_ENV
      value: local
